meta {
  name: List Users
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/users
  body: none
  auth: bearer
}

headers {
  Authorization: Berar {{accessToken}}
}

auth:bearer {
  token: {{accessToken}}
}
